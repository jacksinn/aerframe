AERFRAME v0.1

#Installation
Navigate to site root.
run `composer install`

Create a new MySQL database e.g. Aer
Copy App/Config/example.config.json to App/Config/config.json
Update database parameters in config.json to point to your Aer database

Setup Aer on Apache to point to the public/ directory

Navigate to the Aer site's install.php in the browser to run the Migration and User migrations